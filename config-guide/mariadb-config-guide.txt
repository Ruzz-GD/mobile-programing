Install MariaDB:
Ensure you have MariaDB installed. If you haven't done this yet, run the following command:
-> pkg install mariadb

Running MariaDB:
To start the MariaDB server later, use:
-> mysqld_safe &
-> mysql -u root -p

if ask the password just enter it




